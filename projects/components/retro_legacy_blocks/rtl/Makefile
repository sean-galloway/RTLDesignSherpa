# retro_legacy_blocks lint. All logic lives in rtl/make/area.mk - keep this file at four lines.
AREA     := retro_legacy_blocks
RDS_ROOT := $(if $(REPO_ROOT),$(REPO_ROOT),$(shell git rev-parse --show-toplevel))
include $(RDS_ROOT)/rtl/make/area.mk
