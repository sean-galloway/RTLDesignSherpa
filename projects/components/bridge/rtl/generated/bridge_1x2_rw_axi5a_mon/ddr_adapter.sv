// Slave Adapter: ddr_adapter
// Generated by: SlaveAdapterGenerator
//
// Provides timing isolation and protocol conversion for slave: ddr
// Protocol: AXI5

`timescale 1ns / 1ps

`include "reset_defs.svh"


module ddr_adapter
    import bridge_1x2_rw_axi5a_mon_pkg::*;
#(
    parameter int ID_WIDTH = 4
   ,parameter bit USE_MONITOR_WR = 1'b1
   ,parameter bit USE_MONITOR_RD = 1'b1
) (
    input  logic aclk,
    input  logic aresetn,

    // Crossbar interface (AXI4 from crossbar)
    input  logic [3:0]  xbar_ddr_axi_awid,
    input  logic [31:0]  xbar_ddr_axi_awaddr,
    input  logic [7:0]  xbar_ddr_axi_awlen,
    input  logic [2:0]  xbar_ddr_axi_awsize,
    input  logic [1:0]  xbar_ddr_axi_awburst,
    input  logic         xbar_ddr_axi_awlock,
    input  logic [3:0]  xbar_ddr_axi_awcache,
    input  logic [2:0]  xbar_ddr_axi_awprot,
    input  logic [3:0]  xbar_ddr_axi_awqos,
    input  logic [3:0]  xbar_ddr_axi_awregion,
    input  logic         xbar_ddr_axi_awuser,
    input  logic         xbar_ddr_axi_awvalid,
    output  logic         xbar_ddr_axi_awready,
    input  logic [31:0]  xbar_ddr_axi_wdata,
    input  logic [3:0]  xbar_ddr_axi_wstrb,
    input  logic         xbar_ddr_axi_wlast,
    input  logic         xbar_ddr_axi_wuser,
    input  logic         xbar_ddr_axi_wvalid,
    output  logic         xbar_ddr_axi_wready,
    output  logic [3:0]  xbar_ddr_axi_bid,
    output  logic [1:0]  xbar_ddr_axi_bresp,
    output  logic         xbar_ddr_axi_buser,
    output  logic         xbar_ddr_axi_bvalid,
    input  logic         xbar_ddr_axi_bready,
    input  logic [3:0]  xbar_ddr_axi_arid,
    input  logic [31:0]  xbar_ddr_axi_araddr,
    input  logic [7:0]  xbar_ddr_axi_arlen,
    input  logic [2:0]  xbar_ddr_axi_arsize,
    input  logic [1:0]  xbar_ddr_axi_arburst,
    input  logic         xbar_ddr_axi_arlock,
    input  logic [3:0]  xbar_ddr_axi_arcache,
    input  logic [2:0]  xbar_ddr_axi_arprot,
    input  logic [3:0]  xbar_ddr_axi_arqos,
    input  logic [3:0]  xbar_ddr_axi_arregion,
    input  logic         xbar_ddr_axi_aruser,
    input  logic         xbar_ddr_axi_arvalid,
    output  logic         xbar_ddr_axi_arready,
    output  logic [3:0]  xbar_ddr_axi_rid,
    output  logic [31:0]  xbar_ddr_axi_rdata,
    output  logic [1:0]  xbar_ddr_axi_rresp,
    output  logic         xbar_ddr_axi_rlast,
    output  logic         xbar_ddr_axi_ruser,
    output  logic         xbar_ddr_axi_rvalid,
    input  logic         xbar_ddr_axi_rready,
    // AXI5 native sideband (A5-2 slice 2)
    input  logic         xbar_ddr_axi_awtrace,
    input  logic [5:0]  xbar_ddr_axi_awatop,
    input  logic         xbar_ddr_axi_artrace,
    output logic         xbar_ddr_axi_btrace,
    output logic         xbar_ddr_axi_rtrace,

    // Bridge ID tracking signals
    input  logic [BRIDGE_ID_WIDTH-1:0] xbar_bridge_id_aw,
    output logic [BRIDGE_ID_WIDTH-1:0] bid_bridge_id,
    output logic                       bid_valid,

    input  logic [BRIDGE_ID_WIDTH-1:0] xbar_bridge_id_ar,
    output logic [BRIDGE_ID_WIDTH-1:0] rid_bridge_id,
    output logic                       rid_valid,

    // External slave interface (AXI5)
    output  logic [3:0]  ddr_axi_awid,
    output  logic [31:0]  ddr_axi_awaddr,
    output  logic [7:0]  ddr_axi_awlen,
    output  logic [2:0]  ddr_axi_awsize,
    output  logic [1:0]  ddr_axi_awburst,
    output  logic         ddr_axi_awlock,
    output  logic [3:0]  ddr_axi_awcache,
    output  logic [2:0]  ddr_axi_awprot,
    output  logic [3:0]  ddr_axi_awqos,
    output  logic         ddr_axi_awuser,
    output  logic         ddr_axi_awvalid,
    input  logic         ddr_axi_awready,
    output  logic         ddr_axi_awtrace,
    output  logic [5:0]  ddr_axi_awatop,
    output  logic [31:0]  ddr_axi_wdata,
    output  logic [3:0]  ddr_axi_wstrb,
    output  logic         ddr_axi_wlast,
    output  logic         ddr_axi_wuser,
    output  logic         ddr_axi_wvalid,
    input  logic         ddr_axi_wready,
    input  logic [3:0]  ddr_axi_bid,
    input  logic [1:0]  ddr_axi_bresp,
    input  logic         ddr_axi_buser,
    input  logic         ddr_axi_bvalid,
    output  logic         ddr_axi_bready,
    input  logic         ddr_axi_btrace,
    output  logic [3:0]  ddr_axi_arid,
    output  logic [31:0]  ddr_axi_araddr,
    output  logic [7:0]  ddr_axi_arlen,
    output  logic [2:0]  ddr_axi_arsize,
    output  logic [1:0]  ddr_axi_arburst,
    output  logic         ddr_axi_arlock,
    output  logic [3:0]  ddr_axi_arcache,
    output  logic [2:0]  ddr_axi_arprot,
    output  logic [3:0]  ddr_axi_arqos,
    output  logic         ddr_axi_aruser,
    output  logic         ddr_axi_arvalid,
    input  logic         ddr_axi_arready,
    output  logic         ddr_axi_artrace,
    input  logic [3:0]  ddr_axi_rid,
    input  logic [31:0]  ddr_axi_rdata,
    input  logic [1:0]  ddr_axi_rresp,
    input  logic         ddr_axi_rlast,
    input  logic         ddr_axi_ruser,
    input  logic         ddr_axi_rvalid,
    output  logic         ddr_axi_rready,
    input  logic         ddr_axi_rtrace,

    // Shared free-running monitor-time (from monbus_axil4_axil4_group.mon_time_out)
    input  monitor_common_pkg::monbus_timestamp_t i_mon_time,

    // Monitor side-band: wr wrapper
    output logic                                  monbus_wr_valid,
    input  logic                                  monbus_wr_ready,
    output monitor_common_pkg::monitor_packet_t   monbus_wr_packet,
    output monitor_common_pkg::monbus_timestamp_t monbus_wr_timestamp,

    input  logic         cfg_wr_monitor_enable,
    input  logic         cfg_wr_error_enable,
    input  logic         cfg_wr_timeout_enable,
    input  logic         cfg_wr_perf_enable,
    input  logic         cfg_wr_compl_enable,
    input  logic         cfg_wr_threshold_enable,
    input  logic         cfg_wr_debug_enable,
    input  logic [15:0] cfg_wr_timeout_cycles,
    input  logic [3:0] cfg_wr_freq_sel,
    input  logic [31:0] cfg_wr_latency_threshold,
    input  logic [15:0] cfg_wr_axi_pkt_mask,
    input  logic [15:0] cfg_wr_axi_err_select,
    input  logic [15:0] cfg_wr_axi_error_mask,
    input  logic [15:0] cfg_wr_axi_timeout_mask,
    input  logic [15:0] cfg_wr_axi_compl_mask,
    input  logic [15:0] cfg_wr_axi_thresh_mask,
    input  logic [15:0] cfg_wr_axi_perf_mask,
    input  logic [15:0] cfg_wr_axi_addr_mask,
    input  logic [15:0] cfg_wr_axi_debug_mask,

    // Monitor side-band: rd wrapper
    output logic                                  monbus_rd_valid,
    input  logic                                  monbus_rd_ready,
    output monitor_common_pkg::monitor_packet_t   monbus_rd_packet,
    output monitor_common_pkg::monbus_timestamp_t monbus_rd_timestamp,

    input  logic         cfg_rd_monitor_enable,
    input  logic         cfg_rd_error_enable,
    input  logic         cfg_rd_timeout_enable,
    input  logic         cfg_rd_perf_enable,
    input  logic         cfg_rd_compl_enable,
    input  logic         cfg_rd_threshold_enable,
    input  logic         cfg_rd_debug_enable,
    input  logic [15:0] cfg_rd_timeout_cycles,
    input  logic [3:0] cfg_rd_freq_sel,
    input  logic [31:0] cfg_rd_latency_threshold,
    input  logic [15:0] cfg_rd_axi_pkt_mask,
    input  logic [15:0] cfg_rd_axi_err_select,
    input  logic [15:0] cfg_rd_axi_error_mask,
    input  logic [15:0] cfg_rd_axi_timeout_mask,
    input  logic [15:0] cfg_rd_axi_compl_mask,
    input  logic [15:0] cfg_rd_axi_thresh_mask,
    input  logic [15:0] cfg_rd_axi_perf_mask,
    input  logic [15:0] cfg_rd_axi_addr_mask,
    input  logic [15:0] cfg_rd_axi_debug_mask
);

    // ================================================================
    // Internal Signals
    // ================================================================

    // FIFO tracking signals (in-order mode)
    logic cam_wr_allocate;
    logic cam_wr_deallocate;
    logic [ID_WIDTH-1:0] cam_wr_allocate_tag;
    logic [BRIDGE_ID_WIDTH-1:0] cam_wr_allocate_data;
    logic [ID_WIDTH-1:0] cam_wr_deallocate_tag;

    // A5-3b read-return atomic tracking (u_atom_rd, read section)
    logic atom_hit;
    logic [BRIDGE_ID_WIDTH-1:0] atom_bridge_id;
    logic atom_full;
    logic aw_rr_blocked;

    logic cam_rd_allocate;
    logic cam_rd_deallocate;
    logic [ID_WIDTH-1:0] cam_rd_allocate_tag;
    logic [BRIDGE_ID_WIDTH-1:0] cam_rd_allocate_data;
    logic [ID_WIDTH-1:0] cam_rd_deallocate_tag;

    // ================================================================
    // Bridge ID Tracking - FIFO Mode (In-Order)
    // ================================================================

    // Write Channel FIFO (In-Order) - AXI4 Protocol
    // BRIDGE-011 not-full gating: w_sub_awready is the sub-block's
    // own ready, masked before it reaches the crossbar.
    logic wr_trk_full;
    logic w_sub_awready;
    localparam WR_FIFO_DEPTH = 16;
    logic [BRIDGE_ID_WIDTH-1:0] wr_fifo [WR_FIFO_DEPTH];
    logic [$clog2(WR_FIFO_DEPTH):0] wr_ptr, rd_ptr;

    // Push on AW (crossbar → adapter)
    `ALWAYS_FF_RST(aclk, aresetn,
        if (`RST_ASSERTED(aresetn)) begin
            wr_ptr <= '0;
        end else if (xbar_ddr_axi_awvalid && xbar_ddr_axi_awready) begin
            wr_fifo[wr_ptr[$clog2(WR_FIFO_DEPTH)-1:0]] <= xbar_bridge_id_aw;
            wr_ptr <= wr_ptr + 1'b1;
        end
    )

    // Pop on B response (xbar_ddr_axi_bvalid && xbar_ddr_axi_bready)
    `ALWAYS_FF_RST(aclk, aresetn,
        if (`RST_ASSERTED(aresetn)) begin
            rd_ptr <= '0;
        end else if (xbar_ddr_axi_bvalid && xbar_ddr_axi_bready) begin
            rd_ptr <= rd_ptr + 1'b1;
        end
    )

    // bid_bridge_id / bid_valid drive the crossbar's response mux,
    // which gates B going BACK to the master on bid_valid. Earlier
    // versions registered these on the handshake completing — but
    // the handshake CAN'T complete until the master sees bvalid,
    // and the master can't see bvalid until bid_valid is high.
    // Result: deadlock. Drive these combinationally so the route
    // is open from the moment a B arrives.
    assign bid_bridge_id = wr_fifo[rd_ptr[$clog2(WR_FIFO_DEPTH)-1:0]];
    assign bid_valid     = (wr_ptr != rd_ptr);

    // BRIDGE-011: this FIFO routes B by POSITION, so overrunning it
    // misroutes responses -- past WR_FIFO_DEPTH a live entry is
    // overwritten and its B goes to the wrong master; at twice the
    // depth the pointers lap, (wr_ptr != rd_ptr) reads EMPTY and the
    // response is never routed at all. Gate the AW handshake on
    // not-full in BOTH directions. Draining never depends on
    // accepting a further AW, so this cannot deadlock.
    assign wr_trk_full = (wr_ptr[$clog2(WR_FIFO_DEPTH)] != rd_ptr[$clog2(WR_FIFO_DEPTH)]) &&
                         (wr_ptr[$clog2(WR_FIFO_DEPTH)-1:0] == rd_ptr[$clog2(WR_FIFO_DEPTH)-1:0]);
    // A5-3b: a read-return atomic also needs a slot in the atomic return
    // tracker (u_atom_rd); hold its AW while that is full. Plain writes
    // and store-class atomics are unaffected.
    assign aw_rr_blocked = xbar_ddr_axi_awatop[5] && atom_full;
    assign xbar_ddr_axi_awready = w_sub_awready && !wr_trk_full && !aw_rr_blocked;

    // BRIDGE-010: this port routes B by FIFO POSITION, so it REQUIRES
    // the slave to return B in AW order across all IDs. AXI4 permits a
    // slave to reorder between IDs; such a slave silently misroutes
    // here. Nothing detected that, so record the AWID alongside the
    // master id and check the returned BID against the head. Sim-only:
    // it is a contract check on the attached slave, not logic the
    // bridge needs, and it must cost no gates.
`ifndef SYNTHESIS
    // synthesis translate_off
    logic [4-1:0] wr_id_fifo [WR_FIFO_DEPTH];
    `ALWAYS_FF_RST(aclk, aresetn,
        if (`RST_ASSERTED(aresetn)) begin
        end else begin
            if (xbar_ddr_axi_awvalid && xbar_ddr_axi_awready)
                wr_id_fifo[wr_ptr[$clog2(WR_FIFO_DEPTH)-1:0]] <= xbar_ddr_axi_awid;
            if (xbar_ddr_axi_bvalid && xbar_ddr_axi_bready) begin
                if (xbar_ddr_axi_bid !== wr_id_fifo[rd_ptr[$clog2(WR_FIFO_DEPTH)-1:0]]) begin
                    $error({"BRIDGE-010: slave returned B out of AW order -- ",
                            "got BID=%0h, expected %0h. This bridge routes ",
                            "responses by FIFO position and does not support ",
                            "ID-based reordering; the response has gone to the ",
                            "wrong master."}, xbar_ddr_axi_bid,
                           wr_id_fifo[rd_ptr[$clog2(WR_FIFO_DEPTH)-1:0]]);
                end
            end
        end
    )
    // synthesis translate_on
`endif

    // Read Channel FIFO (In-Order) - AXI4 Protocol
    // BRIDGE-011 not-full gating -- see the write channel.
    logic rd_trk_full;
    logic w_sub_arready;
    localparam RD_FIFO_DEPTH = 16;
    logic [BRIDGE_ID_WIDTH-1:0] rd_fifo [RD_FIFO_DEPTH];
    logic [$clog2(RD_FIFO_DEPTH):0] ar_ptr, r_ptr;

    // Push on AR (crossbar → adapter)
    `ALWAYS_FF_RST(aclk, aresetn,
        if (`RST_ASSERTED(aresetn)) begin
            ar_ptr <= '0;
        end else if (xbar_ddr_axi_arvalid && xbar_ddr_axi_arready) begin
            rd_fifo[ar_ptr[$clog2(RD_FIFO_DEPTH)-1:0]] <= xbar_bridge_id_ar;
            ar_ptr <= ar_ptr + 1'b1;
        end
    )

    // Pop on R response (xbar_ddr_axi_rvalid && xbar_ddr_axi_rready && xbar_ddr_axi_rlast && !atom_hit)
    `ALWAYS_FF_RST(aclk, aresetn,
        if (`RST_ASSERTED(aresetn)) begin
            r_ptr <= '0;
        end else if (xbar_ddr_axi_rvalid && xbar_ddr_axi_rready && xbar_ddr_axi_rlast && !atom_hit) begin
            r_ptr <= r_ptr + 1'b1;
        end
    )

    // rid_bridge_id / rid_valid drive the crossbar's response mux,
    // which gates R going BACK to the master on rid_valid. Earlier
    // versions registered these on the handshake completing — but
    // the handshake CAN'T complete until the master sees rvalid,
    // and the master can't see rvalid until rid_valid is high.
    // Result: deadlock. Drive these combinationally so the route
    // is open from the moment an R arrives.
    // A5-3b: read-return atomics answer on R with their AW's ID and no
    // AR. Track them per ID from the AW handshake. An R beat whose RID
    // hits here is routed by the tracker and leaves the read tracker
    // untouched; every other beat routes as before.
    axi5_atomic_rr_tracker #(
        .AXI_ID_WIDTH(ID_WIDTH),
        .DATA_WIDTH(BRIDGE_ID_WIDTH),
        .DEPTH(8)
    ) u_atom_rd (
        .aclk(aclk),
        .aresetn(aresetn),
        .alloc(xbar_ddr_axi_awvalid && xbar_ddr_axi_awready && xbar_ddr_axi_awatop[5]),
        .alloc_id(xbar_ddr_axi_awid),
        .alloc_data(xbar_bridge_id_aw),
        .full(atom_full),
        .lookup_id(xbar_ddr_axi_rid),
        .hit(atom_hit),
        .hit_data(atom_bridge_id),
        .release_beat(xbar_ddr_axi_rvalid && xbar_ddr_axi_rready && xbar_ddr_axi_rlast)
    );

    assign rid_bridge_id = atom_hit ? atom_bridge_id
                                    : rd_fifo[r_ptr[$clog2(RD_FIFO_DEPTH)-1:0]];
    assign rid_valid     = atom_hit || (ar_ptr != r_ptr);

    // BRIDGE-011, read side -- see the write comment above.
    assign rd_trk_full = (ar_ptr[$clog2(RD_FIFO_DEPTH)] != r_ptr[$clog2(RD_FIFO_DEPTH)]) &&
                         (ar_ptr[$clog2(RD_FIFO_DEPTH)-1:0] == r_ptr[$clog2(RD_FIFO_DEPTH)-1:0]);
    assign xbar_ddr_axi_arready = w_sub_arready && !rd_trk_full;

    // BRIDGE-010, read side -- see the write channel. Checked on the
    // LAST beat, since that is when the FIFO entry is retired.
`ifndef SYNTHESIS
    // synthesis translate_off
    logic [4-1:0] rd_id_fifo [RD_FIFO_DEPTH];
    `ALWAYS_FF_RST(aclk, aresetn,
        if (`RST_ASSERTED(aresetn)) begin
        end else begin
            if (xbar_ddr_axi_arvalid && xbar_ddr_axi_arready)
                rd_id_fifo[ar_ptr[$clog2(RD_FIFO_DEPTH)-1:0]] <= xbar_ddr_axi_arid;
            if (xbar_ddr_axi_rvalid && xbar_ddr_axi_rready && xbar_ddr_axi_rlast && !atom_hit) begin
                if (xbar_ddr_axi_rid !== rd_id_fifo[r_ptr[$clog2(RD_FIFO_DEPTH)-1:0]]) begin
                    $error({"BRIDGE-010: slave returned R out of AR order -- ",
                            "got RID=%0h, expected %0h. This bridge routes ",
                            "responses by FIFO position and does not support ",
                            "ID-based reordering; the data has gone to the ",
                            "wrong master."}, xbar_ddr_axi_rid,
                           rd_id_fifo[r_ptr[$clog2(RD_FIFO_DEPTH)-1:0]]);
                end
            end
        end
    )
    // synthesis translate_on
`endif

    // AXI5 Master Write Timing Wrapper
    axi5_master_wr_mon #(
        .SKID_DEPTH_AW(2),
        .SKID_DEPTH_W(4),
        .SKID_DEPTH_B(2),
        .AXI_ID_WIDTH(4),
        .AXI_ADDR_WIDTH(32),
        .AXI_DATA_WIDTH(32),
        .AXI_USER_WIDTH(1),
        .ENABLE_ATOMIC(1'b1),
        .ENABLE_NSAID(1'b0),
        .ENABLE_TRACE(1'b1),
        .ENABLE_MPAM(1'b0),
        .ENABLE_MECID(1'b0),
        .ENABLE_UNIQUE(1'b0),
        .ENABLE_MTE(1'b0),
        .ENABLE_POISON(1'b0),
        .UNIT_ID(1),
        .AGENT_ID(1),
        .USE_MONITOR(USE_MONITOR_WR),
        .ENABLE_ERROR_LOGIC(1'b1),
        .ENABLE_TIMEOUT_LOGIC(1'b0),
        .ENABLE_COMPL_LOGIC(1'b1),
        .ENABLE_THRESHOLD_LOGIC(1'b0),
        .ENABLE_PERF_LOGIC(1'b0),
        .ENABLE_DEBUG_LOGIC(1'b0)
    ) u_master_wr (
        .aclk(aclk),
        .aresetn(aresetn),

        // Bridge-internal side (fub_axi)
        .fub_axi_awid(xbar_ddr_axi_awid),
        .fub_axi_awaddr(xbar_ddr_axi_awaddr),
        .fub_axi_awlen(xbar_ddr_axi_awlen),
        .fub_axi_awsize(xbar_ddr_axi_awsize),
        .fub_axi_awburst(xbar_ddr_axi_awburst),
        .fub_axi_awlock(xbar_ddr_axi_awlock),
        .fub_axi_awcache(xbar_ddr_axi_awcache),
        .fub_axi_awprot(xbar_ddr_axi_awprot),
        .fub_axi_awqos(xbar_ddr_axi_awqos),
        .fub_axi_awuser(xbar_ddr_axi_awuser),
        .fub_axi_awvalid(xbar_ddr_axi_awvalid && !wr_trk_full && !aw_rr_blocked),
        .fub_axi_awready(w_sub_awready),
        .fub_axi_awatop(xbar_ddr_axi_awatop),
        .fub_axi_awnsaid('0),
        .fub_axi_awtrace(xbar_ddr_axi_awtrace),
        .fub_axi_awmpam('0),
        .fub_axi_awmecid('0),
        .fub_axi_awunique('0),
        .fub_axi_awtagop('0),
        .fub_axi_awtag('0),
        .fub_axi_wdata(xbar_ddr_axi_wdata),
        .fub_axi_wstrb(xbar_ddr_axi_wstrb),
        .fub_axi_wlast(xbar_ddr_axi_wlast),
        .fub_axi_wuser(xbar_ddr_axi_wuser),
        .fub_axi_wvalid(xbar_ddr_axi_wvalid),
        .fub_axi_wready(xbar_ddr_axi_wready),
        .fub_axi_wpoison('0),
        .fub_axi_wtag('0),
        .fub_axi_wtagupdate('0),
        .fub_axi_bid(xbar_ddr_axi_bid),
        .fub_axi_bresp(xbar_ddr_axi_bresp),
        .fub_axi_buser(xbar_ddr_axi_buser),
        .fub_axi_bvalid(xbar_ddr_axi_bvalid),
        .fub_axi_bready(xbar_ddr_axi_bready),
        .fub_axi_btrace(xbar_ddr_axi_btrace),
        .fub_axi_btag(),
        .fub_axi_btagmatch(),

        // External side (m_axi)
        .m_axi_awid(ddr_axi_awid),
        .m_axi_awaddr(ddr_axi_awaddr),
        .m_axi_awlen(ddr_axi_awlen),
        .m_axi_awsize(ddr_axi_awsize),
        .m_axi_awburst(ddr_axi_awburst),
        .m_axi_awlock(ddr_axi_awlock),
        .m_axi_awcache(ddr_axi_awcache),
        .m_axi_awprot(ddr_axi_awprot),
        .m_axi_awqos(ddr_axi_awqos),
        .m_axi_awuser(ddr_axi_awuser),
        .m_axi_awvalid(ddr_axi_awvalid),
        .m_axi_awready(ddr_axi_awready),
        .m_axi_awatop(ddr_axi_awatop),
        .m_axi_awnsaid(),
        .m_axi_awtrace(ddr_axi_awtrace),
        .m_axi_awmpam(),
        .m_axi_awmecid(),
        .m_axi_awunique(),
        .m_axi_awtagop(),
        .m_axi_awtag(),
        .m_axi_wdata(ddr_axi_wdata),
        .m_axi_wstrb(ddr_axi_wstrb),
        .m_axi_wlast(ddr_axi_wlast),
        .m_axi_wuser(ddr_axi_wuser),
        .m_axi_wvalid(ddr_axi_wvalid),
        .m_axi_wready(ddr_axi_wready),
        .m_axi_wpoison(),
        .m_axi_wtag(),
        .m_axi_wtagupdate(),
        .m_axi_bid(ddr_axi_bid),
        .m_axi_bresp(ddr_axi_bresp),
        .m_axi_buser(ddr_axi_buser),
        .m_axi_bvalid(ddr_axi_bvalid),
        .m_axi_bready(ddr_axi_bready),
        .m_axi_btrace(ddr_axi_btrace),
        .m_axi_btag('0),
        .m_axi_btagmatch('0),

        // Status (empty connector = unconnected tie-off)
        .busy(),
        .active_transactions(),
        .error_count(),
        .transaction_count(),
        .cfg_conflict_error(),

        // Monitor bus output
        .i_mon_time(i_mon_time),
        .monbus_valid(monbus_wr_valid),
        .monbus_ready(monbus_wr_ready),
        .monbus_packet(monbus_wr_packet),
        .monbus_timestamp(monbus_wr_timestamp),

        // Monitor cfg inputs
        .cfg_monitor_enable(cfg_wr_monitor_enable),
        .cfg_error_enable(cfg_wr_error_enable),
        .cfg_timeout_enable(cfg_wr_timeout_enable),
        .cfg_perf_enable(cfg_wr_perf_enable),
        .cfg_compl_enable(cfg_wr_compl_enable),
        .cfg_threshold_enable(cfg_wr_threshold_enable),
        .cfg_debug_enable(cfg_wr_debug_enable),
        .cfg_timeout_cycles(cfg_wr_timeout_cycles),
        .cfg_freq_sel(cfg_wr_freq_sel),
        .cfg_latency_threshold(cfg_wr_latency_threshold),
        .cfg_axi_pkt_mask(cfg_wr_axi_pkt_mask),
        .cfg_axi_err_select(cfg_wr_axi_err_select),
        .cfg_axi_error_mask(cfg_wr_axi_error_mask),
        .cfg_axi_timeout_mask(cfg_wr_axi_timeout_mask),
        .cfg_axi_compl_mask(cfg_wr_axi_compl_mask),
        .cfg_axi_thresh_mask(cfg_wr_axi_thresh_mask),
        .cfg_axi_perf_mask(cfg_wr_axi_perf_mask),
        .cfg_axi_addr_mask(cfg_wr_axi_addr_mask),
        .cfg_axi_debug_mask(cfg_wr_axi_debug_mask),

        // Monitor filter/CAM inputs (inert)
        .cam_clear(1'b0),
        .cfg_addr_filter_enable(1'b0),
        .cfg_addr_filter_low('0),
        .cfg_addr_filter_high('0),
        .cfg_id_filter_enable(1'b0),
        .cfg_id_match_base('0),
        .cfg_id_match_count('0),
        .debug_block_ready(),

        // Address-range checker (disabled at N_ADDR_RANGES=0)
        .cfg_addr_check_enable(1'b0),
        .cfg_addr_range_enable(1'b0),
        .cfg_addr_range_low({32{1'b0}}),
        .cfg_addr_range_high({32{1'b0}}),

        // Perfmon Stage A/B (tied off -- no window driven)
        .cfg_start_event_sel(3'b111),
        .cfg_end_event_sel(3'b111),
        .cfg_start_trigger(1'b0),
        .cfg_end_trigger(1'b0),
        .cfg_window_force_close(1'b0),
        .window_active(),
        .window_cycles(),
        .perf_prod_cycles(),
        .perf_bp_cycles(),
        .perf_starv_cycles(),
        .perf_idle_cycles(),
        .perf_beat_count(),
        .perf_byte_count(),
        .perf_burst_count()
    );

    // AXI5 Master Read Timing Wrapper
    axi5_master_rd_mon #(
        .SKID_DEPTH_AR(2),
        .SKID_DEPTH_R(2),
        .AXI_ID_WIDTH(4),
        .AXI_ADDR_WIDTH(32),
        .AXI_DATA_WIDTH(32),
        .AXI_USER_WIDTH(1),
        .ENABLE_NSAID(1'b0),
        .ENABLE_TRACE(1'b1),
        .ENABLE_MPAM(1'b0),
        .ENABLE_MECID(1'b0),
        .ENABLE_UNIQUE(1'b0),
        .ENABLE_CHUNKING(1'b0),
        .ENABLE_MTE(1'b0),
        .ENABLE_POISON(1'b0),
        .UNIT_ID(1),
        .AGENT_ID(0),
        .USE_MONITOR(USE_MONITOR_RD),
        .ENABLE_ERROR_LOGIC(1'b1),
        .ENABLE_TIMEOUT_LOGIC(1'b0),
        .ENABLE_COMPL_LOGIC(1'b1),
        .ENABLE_THRESHOLD_LOGIC(1'b0),
        .ENABLE_PERF_LOGIC(1'b0),
        .ENABLE_DEBUG_LOGIC(1'b0)
    ) u_master_rd (
        .aclk(aclk),
        .aresetn(aresetn),

        // Bridge-internal side (fub_axi)
        .fub_axi_arid(xbar_ddr_axi_arid),
        .fub_axi_araddr(xbar_ddr_axi_araddr),
        .fub_axi_arlen(xbar_ddr_axi_arlen),
        .fub_axi_arsize(xbar_ddr_axi_arsize),
        .fub_axi_arburst(xbar_ddr_axi_arburst),
        .fub_axi_arlock(xbar_ddr_axi_arlock),
        .fub_axi_arcache(xbar_ddr_axi_arcache),
        .fub_axi_arprot(xbar_ddr_axi_arprot),
        .fub_axi_arqos(xbar_ddr_axi_arqos),
        .fub_axi_aruser(xbar_ddr_axi_aruser),
        .fub_axi_arvalid(xbar_ddr_axi_arvalid && !rd_trk_full),
        .fub_axi_arready(w_sub_arready),
        .fub_axi_arnsaid('0),
        .fub_axi_artrace(xbar_ddr_axi_artrace),
        .fub_axi_armpam('0),
        .fub_axi_armecid('0),
        .fub_axi_arunique('0),
        .fub_axi_archunken('0),
        .fub_axi_artagop('0),
        .fub_axi_rid(xbar_ddr_axi_rid),
        .fub_axi_rdata(xbar_ddr_axi_rdata),
        .fub_axi_rresp(xbar_ddr_axi_rresp),
        .fub_axi_rlast(xbar_ddr_axi_rlast),
        .fub_axi_ruser(xbar_ddr_axi_ruser),
        .fub_axi_rvalid(xbar_ddr_axi_rvalid),
        .fub_axi_rready(xbar_ddr_axi_rready),
        .fub_axi_rtrace(xbar_ddr_axi_rtrace),
        .fub_axi_rpoison(),
        .fub_axi_rchunkv(),
        .fub_axi_rchunknum(),
        .fub_axi_rchunkstrb(),
        .fub_axi_rtag(),
        .fub_axi_rtagmatch(),

        // External side (m_axi)
        .m_axi_arid(ddr_axi_arid),
        .m_axi_araddr(ddr_axi_araddr),
        .m_axi_arlen(ddr_axi_arlen),
        .m_axi_arsize(ddr_axi_arsize),
        .m_axi_arburst(ddr_axi_arburst),
        .m_axi_arlock(ddr_axi_arlock),
        .m_axi_arcache(ddr_axi_arcache),
        .m_axi_arprot(ddr_axi_arprot),
        .m_axi_arqos(ddr_axi_arqos),
        .m_axi_aruser(ddr_axi_aruser),
        .m_axi_arvalid(ddr_axi_arvalid),
        .m_axi_arready(ddr_axi_arready),
        .m_axi_arnsaid(),
        .m_axi_artrace(ddr_axi_artrace),
        .m_axi_armpam(),
        .m_axi_armecid(),
        .m_axi_arunique(),
        .m_axi_archunken(),
        .m_axi_artagop(),
        .m_axi_rid(ddr_axi_rid),
        .m_axi_rdata(ddr_axi_rdata),
        .m_axi_rresp(ddr_axi_rresp),
        .m_axi_rlast(ddr_axi_rlast),
        .m_axi_ruser(ddr_axi_ruser),
        .m_axi_rvalid(ddr_axi_rvalid),
        .m_axi_rready(ddr_axi_rready),
        .m_axi_rtrace(ddr_axi_rtrace),
        .m_axi_rpoison('0),
        .m_axi_rchunkv('0),
        .m_axi_rchunknum('0),
        .m_axi_rchunkstrb('0),
        .m_axi_rtag('0),
        .m_axi_rtagmatch('0),

        // Status (empty connector = unconnected tie-off)
        .busy(),
        .active_transactions(),
        .error_count(),
        .transaction_count(),
        .cfg_conflict_error(),

        // Monitor bus output
        .i_mon_time(i_mon_time),
        .monbus_valid(monbus_rd_valid),
        .monbus_ready(monbus_rd_ready),
        .monbus_packet(monbus_rd_packet),
        .monbus_timestamp(monbus_rd_timestamp),

        // Monitor cfg inputs
        .cfg_monitor_enable(cfg_rd_monitor_enable),
        .cfg_error_enable(cfg_rd_error_enable),
        .cfg_timeout_enable(cfg_rd_timeout_enable),
        .cfg_perf_enable(cfg_rd_perf_enable),
        .cfg_compl_enable(cfg_rd_compl_enable),
        .cfg_threshold_enable(cfg_rd_threshold_enable),
        .cfg_debug_enable(cfg_rd_debug_enable),
        .cfg_timeout_cycles(cfg_rd_timeout_cycles),
        .cfg_freq_sel(cfg_rd_freq_sel),
        .cfg_latency_threshold(cfg_rd_latency_threshold),
        .cfg_axi_pkt_mask(cfg_rd_axi_pkt_mask),
        .cfg_axi_err_select(cfg_rd_axi_err_select),
        .cfg_axi_error_mask(cfg_rd_axi_error_mask),
        .cfg_axi_timeout_mask(cfg_rd_axi_timeout_mask),
        .cfg_axi_compl_mask(cfg_rd_axi_compl_mask),
        .cfg_axi_thresh_mask(cfg_rd_axi_thresh_mask),
        .cfg_axi_perf_mask(cfg_rd_axi_perf_mask),
        .cfg_axi_addr_mask(cfg_rd_axi_addr_mask),
        .cfg_axi_debug_mask(cfg_rd_axi_debug_mask),

        // Monitor filter/CAM inputs (inert)
        .cam_clear(1'b0),
        .cfg_addr_filter_enable(1'b0),
        .cfg_addr_filter_low('0),
        .cfg_addr_filter_high('0),
        .cfg_id_filter_enable(1'b0),
        .cfg_id_match_base('0),
        .cfg_id_match_count('0),
        .debug_block_ready(),

        // Address-range checker (disabled at N_ADDR_RANGES=0)
        .cfg_addr_check_enable(1'b0),
        .cfg_addr_range_enable(1'b0),
        .cfg_addr_range_low({32{1'b0}}),
        .cfg_addr_range_high({32{1'b0}}),

        // Perfmon Stage A/B (tied off -- no window driven)
        .cfg_start_event_sel(3'b111),
        .cfg_end_event_sel(3'b111),
        .cfg_start_trigger(1'b0),
        .cfg_end_trigger(1'b0),
        .cfg_window_force_close(1'b0),
        .window_active(),
        .window_cycles(),
        .perf_prod_cycles(),
        .perf_bp_cycles(),
        .perf_starv_cycles(),
        .perf_idle_cycles(),
        .perf_beat_count(),
        .perf_byte_count(),
        .perf_burst_count()
    );

endmodule : ddr_adapter
