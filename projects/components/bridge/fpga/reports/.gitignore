# Evidence stays: the per-run summary, the per-instance utilization and the
# timing summary. The bulky per-path dumps, DRC/CDC/route reports and run
# logs are regenerated by `make synth` and are not worth the diff noise.
*
!.gitignore
!summary.csv
!*/
!*/summary.txt
!*/utilization_hier.txt
!*/timing_summary.txt
