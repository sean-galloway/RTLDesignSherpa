# rapids-fub_beats tests. All logic lives in make/tests.mk (TOOL-008) - keep this file at four lines.
AREA        := rapids-fub_beats
RDS_ROOT    := $(if $(REPO_ROOT),$(REPO_ROOT),$(shell git rev-parse --show-toplevel))
include $(RDS_ROOT)/make/tests.mk
