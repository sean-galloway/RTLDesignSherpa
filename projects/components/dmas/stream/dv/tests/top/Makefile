# stream top-level (stream_top_ch8) tests. All logic lives in make/tests.mk (TOOL-008) - keep this file at four lines.
AREA        := stream-top
RDS_ROOT    := $(if $(REPO_ROOT),$(REPO_ROOT),$(shell git rev-parse --show-toplevel))
include $(RDS_ROOT)/make/tests.mk
