# stream FUB (functional unit block) tests. All logic lives in make/tests.mk (TOOL-008) - keep this file at four lines.
AREA        := stream-fub
RDS_ROOT    := $(if $(REPO_ROOT),$(REPO_ROOT),$(shell git rev-parse --show-toplevel))
include $(RDS_ROOT)/make/tests.mk
