# rtl/integ_common lint. All logic lives in rtl/make/area.mk - keep this file at four lines.
AREA     := integ_common
RDS_ROOT := $(if $(REPO_ROOT),$(REPO_ROOT),$(shell git rev-parse --show-toplevel))
include $(RDS_ROOT)/rtl/make/area.mk
