# Filelist for wb4_pkg (the Wishbone B4 response-status encoding)
# Location: rtl/amba/filelists/wb4_pkg.f
# Consumers outside rtl/amba (the converters) -f this rather than
# hand-listing the package.

$REPO_ROOT/rtl/amba/wb4/wb4_pkg.sv
