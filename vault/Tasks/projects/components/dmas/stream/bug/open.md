<!-- Managed by the `tasks` convention: see /vault/Tasks/INDEX.md. Move an entry between pages by cutting its block, do not copy. -->

# projects/components/dmas/stream bugs — open (not started)

## BUG-000: TEMPLATE — copy this shape, never file against it
**Priority:** P3
**Status:** Not Started
**Owner:** TBD

ID 000 is a reserved dummy in every area and every lane. It exists so the page
carries a recognised entry from day one -- an empty page and a page the checker
cannot parse look identical in a passing run, and this repo has been bitten by
that more than once. Real bugs start at BUG-001.

**What belongs in this lane:** a DEFECT with a reproduction: something behaves wrongly and we can say what correct looks like. If you cannot state the expected behaviour, it is an ISSUE, not a bug.

Cut-and-paste the block above, give it the next ID from `INDEX.md`, bump that
`Next ID:` line, and keep the `**Status:**` line current with a date.

---
